# Base URL of the Digit Properties web app (API + pages).
# Expo bundles this at build time; change and restart "npx expo start" to pick up changes.
#
# --- How to get EXPO_PUBLIC_API_URL ---
#
# 1) Production (app on phone, backend live):
#    Use your deployed site with no path, e.g.:
#    EXPO_PUBLIC_API_URL=https://digitproperties.com
#
# 2) Local development (phone + dev server on your computer):
#    - Start the web app: in the "web" folder run "npm run dev" (e.g. on http://localhost:3000).
#    - Your phone cannot use "localhost"; use your computer's LAN IP and the port, e.g.:
#      EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
#    - To find your IP:
#      Windows (PowerShell):  Get-NetIPAddress -AddressFamily IPv4 | Where-Object InterfaceAlias -notmatch "Loopback"
#      Mac/Linux:             ifconfig | grep "inet " or: ip addr
#    - Ensure phone and computer are on the same Wi‑Fi.
#
EXPO_PUBLIC_API_URL=https://digitproperties.com

# Optional: Social login — use the same App ID / Client ID as in web/.env.local
# EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com
# Facebook: copy FACEBOOK_CLIENT_ID from web/.env.local (same value)
EXPO_PUBLIC_FACEBOOK_APP_ID=2275129516137890
